## 1. Setup Fonts

- [x] 1.1 Download `Plus Jakarta Sans` font files and add them to `app/src/main/res/font/`.
- [x] 1.2 Download `Hanken Grotesk` font files and add them to `app/src/main/res/font/`.
- [x] 1.3 Update `Type.kt` to define `FontFamily` variables for both fonts.

## 2. Implement Colors

- [x] 2.1 Update `Color.kt` to define the new hex codes from the Figma design (e.g. Primary `#4D44E3`, Background `#F7F9FB`, Primary Text `#0B1C30`).
- [x] 2.2 Update `Theme.kt` to map the newly defined colors to the `lightColorScheme` (and `darkColorScheme` if applicable).

## 3. Implement Typography

- [x] 3.1 Update `Type.kt` to map the `Typography` properties (e.g., `displayLarge`, `headlineMedium`, `titleLarge` to `Plus Jakarta Sans`; and `bodyLarge`, `labelSmall` to `Hanken Grotesk`).

## 4. Verification

- [x] 4.1 Update or create `@Preview` functions in a sample screen or component to verify that the new typography and colors are properly applied across the app.
